server:
  port: 8081
  servlet:
    context-path: /admin

spring:
  application:
    name: Game-Account-Shop-Admin

  datasource:
    url: jdbc:mysql://localhost:3306/play_buddy?useUnicode=true&characterEncoding=utf-8&isSSL=false
    username: root
    password: root