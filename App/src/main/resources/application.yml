server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: Game-Account-Shop-App

  datasource:
    url: jdbc:mysql://localhost:3306/play_buddy?useUnicode=true&characterEncoding=utf-8&isSSL=false
    username: root
    password: root